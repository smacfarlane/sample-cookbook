build:
  test: 
    image: uafgina/chefdk
    commands:
      - eval "$(chef shell-init sh)"
      - rake
  upload:
    image: uafgina/chefdk
    commands:
      - eval "$(chef shell-init sh)"
      - berks install
      - berks upload
    when:
      event: [push]
      branch: master
